--- # Managed by Ansible (roles/homepage) — manual edits will be overwritten.
# Intentionally empty - the GitHub repo link lives as a proper service tile
# (Developer group, homepage_static_services in group_vars/homepage.yml)
# instead of being duplicated here.
[]
