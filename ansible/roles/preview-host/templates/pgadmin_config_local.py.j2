# Managed by Ansible (roles/preview-host) — manual edits will be overwritten.
# pgAdmin OAuth2/OIDC login against Authentik (docs/01 ADR 73, tier-1 #5/6).
# Mounted read-only at /pgadmin4/config_local.py - pgAdmin's own documented
# override point for exactly this kind of config too complex for a plain
# env var (unlike PGADMIN_DEFAULT_EMAIL/PASSWORD above).
AUTHENTICATION_SOURCES = ['oauth2', 'internal']

OAUTH2_CONFIG = [{
    'OAUTH2_NAME': 'authentik',
    'OAUTH2_DISPLAY_NAME': 'Authentik',
    'OAUTH2_CLIENT_ID': '{{ pgadmin_oidc_client_id }}',
    'OAUTH2_CLIENT_SECRET': '{{ pgadmin_oidc_client_secret }}',
    'OAUTH2_TOKEN_URL': 'https://authentik.{{ internal_domain }}/application/o/token/',
    'OAUTH2_AUTHORIZATION_URL': 'https://authentik.{{ internal_domain }}/application/o/authorize/',
    'OAUTH2_API_BASE_URL': 'https://authentik.{{ internal_domain }}/',
    'OAUTH2_USERINFO_ENDPOINT': 'application/o/userinfo/',
    'OAUTH2_SERVER_METADATA_URL': 'https://authentik.{{ internal_domain }}/application/o/pgadmin/.well-known/openid-configuration',
    'OAUTH2_SCOPE': 'openid email profile',
    'OAUTH2_ICON': 'fa-key',
    'OAUTH2_BUTTON_COLOR': '#fc6d26',
}]

OAUTH2_AUTO_CREATE_USER = True
