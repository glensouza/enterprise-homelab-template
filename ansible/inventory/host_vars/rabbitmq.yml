---
# RabbitMQ (ADR 07/10). Generated once on the Ansible controller and reused
# on every run - credentials/ is git-ignored, same pattern as pki.yml's
# step_ca_password.
rabbitmq_password: "{{ lookup('ansible.builtin.password', 'credentials/rabbitmq/password length=32 chars=ascii_letters,digits') }}"
